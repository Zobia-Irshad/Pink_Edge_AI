# Pink Edge AI — Python dependencies (Desktop + Streamlit editions share this file).
# Tkinter is stdlib and not listed here. Install with: pip install -r requirements.txt
# (Start.bat does this automatically on first run.)

numpy>=1.24.0
Pillow>=10.0.0
# headless build: the GUI-enabled `opencv-python` needs system graphics libs (libGL.so.1) that
# headless containers like Streamlit Community Cloud don't have. TB inference (CLAHE/blur/resize)
# and ultralytics both work fine against the headless build; nothing here opens an OpenCV window.
opencv-python-headless>=4.8.0
fpdf2>=2.7.0
requests>=2.31.0
streamlit>=1.30.0

# ML runtime — CPU is enough for all 3 models at demo scale.
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
huggingface_hub>=0.20.0

# Optional: only needed if you provide a Roboflow API key for the real mammography model
# (see MODEL_SOURCES.md). Safe to leave installed even if unused.
roboflow>=1.1.0
dicom-anonymizer
